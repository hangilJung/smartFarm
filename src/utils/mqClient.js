// const mqtt = require("mqtt");
// const mqClient = mqtt.connect(process.env.MQTT_BROKER_SERVER_HOST);

// mqClient.on("connect", () => {
//   console.log("MQTT SERVER CONNECTED");
// });

// mqClient.on("error", (err) => {
//   console.log("error : " + err);
// });

// module.exports = mqClient;
